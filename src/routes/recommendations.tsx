/**
 * Route module for `/recommendations` — job and event matches.
 *
 * Exports: RecommendationsRoute (default)
 */

import RecommendationsPage from "@/features/insights/pages/RecommendationsPage";

export default function RecommendationsRoute() {
  return <RecommendationsPage />;
}
