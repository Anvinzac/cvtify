/**
 * Route module for `/dashboard` — experiences dashboard.
 *
 * Exports: DashboardRoute (default)
 */

import DashboardPage from "@/features/insights/pages/DashboardPage";

export default function DashboardRoute() {
  return <DashboardPage />;
}
