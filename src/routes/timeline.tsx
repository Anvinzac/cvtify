/**
 * Route module for `/timeline` — activity timeline view.
 *
 * Exports: TimelineRoute (default)
 */

import TimelinePage from "@/features/activities/pages/TimelinePage";

export default function TimelineRoute() {
  return <TimelinePage />;
}
