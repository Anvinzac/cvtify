/**
 * Route module for `/add-activity/:categoryId` — activity walkthrough.
 *
 * Exports: AddActivityRoute (default)
 */

import AddActivityPage from "@/features/activities/pages/AddActivityPage";

export default function AddActivityRoute() {
  return <AddActivityPage />;
}
