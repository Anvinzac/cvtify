/**
 * Route module for `/dream-jobs` — dream job skill mapping.
 *
 * Exports: DreamJobsRoute (default)
 */

import DreamJobsPage from "@/features/insights/pages/DreamJobsPage";

export default function DreamJobsRoute() {
  return <DreamJobsPage />;
}
